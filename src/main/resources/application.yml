server:
  port: 8088

topic:
  name: t.food.order

spring:
  kafka:
    consumer:
      group-id: "default"
      bootstrap-servers:
        localhost:9092

  h2:
    console:
      enabled: true
      path: /h2-console
  datasource:
    url: jdbc:h2:mem:testdb
    username: sa
    password: password
